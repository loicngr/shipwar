import {
  DIRECTION_DOWN,
  DIRECTION_LEFT,
  DIRECTION_RIGHT,
  DIRECTION_UP,
} from '../const/tile'

export enum DirectionEnum {
  Up = DIRECTION_UP,
  Down = DIRECTION_DOWN,
  Left = DIRECTION_LEFT,
  Right = DIRECTION_RIGHT,
}
